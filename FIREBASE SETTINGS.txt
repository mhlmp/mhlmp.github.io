rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // 1. חסימה מוחלטת כברירת מחדל לכל מסד הנתונים
    // זה מבטיח שאם בעתיד תוסיף אוסף חדש ותשכח לכתוב לו חוקים, הוא יהיה חסום אוטומטית.
    match /{document=**} {
      allow read, write: if false;
    }

    // 2. הגישה למסמכי המשתמש (שם יושבים מפתחות ה-API היקרים)
    match /users/{userId} {
      // רק המשתמש עצמו יכול לקרוא ולערוך את מסמך הפרופיל וההגדרות שלו
      allow read, write: if request.auth != null && request.auth.uid == userId;

      // 3. הגנה על תת-אוסף הקישורים
      match /items/{itemId} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }

      // 4. הגנה על תת-אוסף סל המחזור
      match /trash/{trashId} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }
    }
  }
}